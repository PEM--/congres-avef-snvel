unless isAllSubsReady
  +loaderContent
else
  .content.left-align.fadeInUp
    h3 Sélectionner une session
    p Vous validez les entrées pour la session :
    .input-field.days.col.s12
      select.days
        each dayValues
          option(value=value) {{label}}
    +if isHourSelectable
      .input-field.hours.col.s12
          select.hours
            each hourValues
              option(value=value) {{label}}
    +if isSessionSelectable
      .input-field.sessions.col.s12
          select.sessions
            each sessionValues
              option(value=value) {{label}}
